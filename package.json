{
    "name": "socket-chat-example",
    "version": "0.0.1",
    "description": "my first socket.io app",
"scripts": {
  "start": "node index.js",
  "watch": "browser-sync start --proxy 'localhost:3000' --files 'public/**/*.*' --reload-delay 1000" 
},
    "devDependencies": {
        "browser-sync": "^3.0.4"
    },
    "dependencies": {
        "express": "^4.21.2",
        "socket.io": "^4.8.1"
    }
}
